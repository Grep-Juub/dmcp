# MCP Tools Configuration Example
# Copy this to .env and customize for your setup

# ============================================================================
# EMBEDDING MODEL CONFIGURATION
# ============================================================================
# Any HuggingFace model that works with infinity-emb and provides OpenAI-compatible API
# Model must output embeddings - sentence-transformers, E5, BGE, etc.
# 
# IMPORTANT: Update EMBEDDING_DIMENSIONS in your TypeScript config to match model output!
# - e5-small-v2: 384 dimensions
# - e5-large-v2: 1024 dimensions
# - bge-large-en-v1.5: 1024 dimensions
# 
# Tool-optimized models (recommended for better tool retrieval):
EMBEDDING_MODEL=mangopy/ToolRet-trained-e5-large-v2

# Other options:
# EMBEDDING_MODEL=intfloat/e5-small-v2                    # General purpose, 384 dims
# EMBEDDING_MODEL=intfloat/e5-large-v2                    # General purpose, 1024 dims
# EMBEDDING_MODEL=mangopy/ToolRet-trained-bge-large-en-v1.5  # Tool-optimized, 1024 dims
# EMBEDDING_MODEL=BAAI/bge-small-en-v1.5                  # General purpose, 384 dims
# EMBEDDING_MODEL=BAAI/bge-large-en-v1.5                  # General purpose, 1024 dims

# NOTE: If you change the model, ensure embedding dimensions match:
# Update these files with correct dimensions:
# - mcp-server/src/dmcp-indexer.ts (embeddingDimensions)
# - mcp-server/src/custom-embedding-provider.ts (dimensions default)
# - mcp-server/src/tool-classifier.ts (dimensions default)
# - mcp-server/src/tool-router.ts (dimensions)

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6380
# REDIS_PASSWORD=your-password-here

# ============================================================================
# EMBEDDING SERVICE
# ============================================================================
EMBEDDING_URL=http://localhost:5000

# ============================================================================
# INDEXER OPTIONS
# ============================================================================
# Enable/disable embedding-based classification
CLASSIFY_TOOLS=true
